// var assert = require("assert"); // importing a module / library of code
// var expect = require('chai').expect;

// describe("#indexOf()", function () {
//     it("should return -1 when the value is not present", function () {
//         assert.strictEqual([1, 2, 3].indexOf(4), -1); // The index of 4 in the array doesn't exist, return -1
//         assert.strictEqual([1, 2, 3].indexOf(2), 1);
//     });
//     it("should return 1 when the value 2 is not present", function () {
//         assert.strictEqual([1, 2, 3].indexOf(2), 1); // Index of 2, should return 1
//     });
//     it("should return 1 when the value 2 is not present", function () {
//         assert.strictEqual([1, 0, 3, 2].indexOf(2), 1); // Should fail
//     });
// });
